function searchNum(firstArr, secondArr) {
    let [numsEle, deleteEle, target] = secondArr;

    for (let i = 0; i < firstArr.length; i++) {
        firstArr
    }

    console.log(numsEle);
}
searchNum([5, 2, 3, 4, 1, 6], [5, 2, 3])